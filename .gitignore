# Quarto intermediate artifacts — embedded in the .html already
*_files/
*.knit.md
*.quarto_ipynb
.quarto/

# Permit + parcel RDS cache (recreated on render)
.cache/

# Private data folders (not meant for public deploy)
NonResData/
MFResData/
MultiResData/

# Cross-reference reports (depend on private sales data)
*CrossRef.*

# CSV exports from DT download buttons
wpg_*_permits_filtered.csv

# R / RStudio
.Rproj.user/
.Rhistory
.RData
.Ruserdata
*.Rproj

# Claude Code session state
.claude/

# OS / editor
.DS_Store
Thumbs.db

# Local-only context docs
HANDOFF.md

# Unrelated local appraisal dashboard — not part of the published permits
# site (contains hardcoded local paths). Kept locally, never deployed.
LandV2.3.qmd

# Tmp / backup files
*.tmp
*.bak
HANDOFF.md.tmp.*

# Local Vercel build output
.vercel/
.Rproj.user
.positai
.Rproj.user
.positai
